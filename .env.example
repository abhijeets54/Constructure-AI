# ============================================================================
# BACKEND CONFIGURATION (.env file for backend)
# ============================================================================

# ------------------------------------------------------------------------------
# Google OAuth2 Configuration
# ------------------------------------------------------------------------------
# To get these credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Gmail API:
#    - Go to "APIs & Services" > "Library"
#    - Search for "Gmail API" and enable it
# 4. Create OAuth2 credentials:
#    - Go to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "OAuth client ID"
#    - Choose "Web application"
#    - Add Authorized redirect URIs:
#      * For local development: http://localhost:8000/auth/callback
#      * For production: https://your-backend-url.vercel.app/auth/callback
# 5. Copy the Client ID and Client Secret below
# 6. IMPORTANT: Add test users under "OAuth consent screen" > "Test users"
#    - Add: test@gmail.com (or testingcheckuser1234@gmail.com as specified in requirements)

GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Redirect URI must match what you configured in Google Console
# Local development:
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback
# Production (update with your actual backend URL):
# GOOGLE_REDIRECT_URI=https://your-backend-url.vercel.app/auth/callback

# ------------------------------------------------------------------------------
# JWT Configuration
# ------------------------------------------------------------------------------
# Generate a secure random string for JWT_SECRET_KEY:
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_super_secure_random_jwt_secret_key_here_please_change_this
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ------------------------------------------------------------------------------
# AI PROVIDER CONFIGURATION - CHOOSE ONE!
# ------------------------------------------------------------------------------
# Options: "gemini" (FREE!) or "openai" (requires payment)
#
# 🌟 RECOMMENDED: Use "gemini" for FREE testing!
# 💰 For submission: Keep as "gemini" or change to "openai" if evaluator has keys
AI_PROVIDER=gemini

# ------------------------------------------------------------------------------
# OPTION 1: Google Gemini (FREE!) - Recommended for Development & Testing
# ------------------------------------------------------------------------------
# Get your FREE Gemini API key:
# 1. Go to: https://makersuite.google.com/app/apikey
# 2. Click "Create API Key"
# 3. Copy the key below
#
# 🎉 This is 100% FREE and works great!
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
# Other free options: gemini-1.5-pro (more capable), gemini-1.5-flash (fastest)

# ------------------------------------------------------------------------------
# OPTION 2: OpenAI (Requires Paid API Key)
# ------------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# 💰 Note: OpenAI charges per token usage
#
# Only fill this in if you set AI_PROVIDER=openai above
# Otherwise, leave blank or comment out
# OPENAI_API_KEY=sk-your_openai_api_key_here
# OPENAI_MODEL=gpt-4o-mini

# ------------------------------------------------------------------------------
# Application URLs
# ------------------------------------------------------------------------------
# Local development:
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000

# Production (update with your actual URLs):
# FRONTEND_URL=https://your-app-name.vercel.app
# BACKEND_URL=https://your-backend-url.vercel.app

ENVIRONMENT=development
# For production: ENVIRONMENT=production

# ============================================================================
# FRONTEND CONFIGURATION (.env.local file for frontend)
# ============================================================================
# Create a separate .env.local file in the frontend/ directory with:

# NEXT_PUBLIC_API_URL=http://localhost:8000
# For production:
# NEXT_PUBLIC_API_URL=https://your-backend-url.vercel.app


# ============================================================================
# QUICK SETUP SUMMARY
# ============================================================================
#
# FOR FREE TESTING (Recommended):
# 1. Get Google OAuth credentials (above)
# 2. Get Gemini API key: https://makersuite.google.com/app/apikey
# 3. Set AI_PROVIDER=gemini
# 4. Fill in GEMINI_API_KEY
# 5. Done! No costs involved!
#
# FOR EVALUATOR WITH OPENAI KEYS:
# 1. Get Google OAuth credentials (above)
# 2. Set AI_PROVIDER=openai
# 3. Evaluator fills in their OPENAI_API_KEY
# 4. Code works identically!
#
# ============================================================================
